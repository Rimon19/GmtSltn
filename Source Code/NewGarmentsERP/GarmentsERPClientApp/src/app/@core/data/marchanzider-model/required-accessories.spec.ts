import { RequiredAccessories } from './required-accessories';

describe('RequiredAccessories', () => {
  it('should create an instance', () => {
    expect(new RequiredAccessories()).toBeTruthy();
  });
});
