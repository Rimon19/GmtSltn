import { SizeWisePannelPodetails } from './size-wise-pannel-podetails.model';

describe('SizeWisePannelPodetails', () => {
  it('should create an instance', () => {
    expect(new SizeWisePannelPodetails()).toBeTruthy();
  });
});
