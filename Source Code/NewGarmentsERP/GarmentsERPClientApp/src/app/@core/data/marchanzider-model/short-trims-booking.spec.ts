import { ShortTrimsBooking } from './short-trims-booking';

describe('ShortTrimsBooking', () => {
  it('should create an instance', () => {
    expect(new ShortTrimsBooking()).toBeTruthy();
  });
});
