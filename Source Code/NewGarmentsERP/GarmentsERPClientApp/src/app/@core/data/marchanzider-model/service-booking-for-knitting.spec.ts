import { ServiceBookingForKnitting } from './service-booking-for-knitting';

describe('ServiceBookingForKnitting', () => {
  it('should create an instance', () => {
    expect(new ServiceBookingForKnitting()).toBeTruthy();
  });
});
