import { YarnServiceWorkOrderDetails } from './yarn-service-work-order-details';

describe('YarnServiceWorkOrderDetails', () => {
  it('should create an instance', () => {
    expect(new YarnServiceWorkOrderDetails()).toBeTruthy();
  });
});
