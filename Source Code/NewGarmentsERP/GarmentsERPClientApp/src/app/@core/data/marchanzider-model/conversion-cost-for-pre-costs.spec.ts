import { ConversionCostForPreCosts } from './conversion-cost-for-pre-costs';

describe('ConversionCostForPreCosts', () => {
  it('should create an instance', () => {
    expect(new ConversionCostForPreCosts()).toBeTruthy();
  });
});
