import { SalesForecastEntryDetails } from './sales-forecast-entry-details.model';

describe('SalesForecastEntryDetails', () => {
  it('should create an instance', () => {
    expect(new SalesForecastEntryDetails()).toBeTruthy();
  });
});
