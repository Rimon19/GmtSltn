import { SampleRequisitionWithBooking } from './sample-requisition-with-booking';

describe('SampleRequisitionWithBooking', () => {
  it('should create an instance', () => {
    expect(new SampleRequisitionWithBooking()).toBeTruthy();
  });
});
