import { SampleDevelopmentEntry } from './sample-development-entry.model';

describe('SampleDevelopmentEntry', () => {
  it('should create an instance', () => {
    expect(new SampleDevelopmentEntry()).toBeTruthy();
  });
});
