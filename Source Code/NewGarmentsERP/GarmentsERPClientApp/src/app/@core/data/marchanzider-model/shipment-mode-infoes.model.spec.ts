import { ShipmentModeInfoes } from './shipment-mode-infoes.model';

describe('ShipmentModeInfoes', () => {
  it('should create an instance', () => {
    expect(new ShipmentModeInfoes()).toBeTruthy();
  });
});
