import { YarnServiceWorkOrder } from './yarn-service-work-order';

describe('YarnServiceWorkOrder', () => {
  it('should create an instance', () => {
    expect(new YarnServiceWorkOrder()).toBeTruthy();
  });
});
