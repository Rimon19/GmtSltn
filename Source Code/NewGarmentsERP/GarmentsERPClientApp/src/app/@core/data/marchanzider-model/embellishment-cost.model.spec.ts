import { EmbellishmentCost } from './embellishment-cost.model';

describe('EmbellishmentCost', () => {
  it('should create an instance', () => {
    expect(new EmbellishmentCost()).toBeTruthy();
  });
});
