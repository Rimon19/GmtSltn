import { EmpAdditionalDesignation } from './emp-additional-designation';

describe('EmpAdditionalDesignation', () => {
  it('should create an instance', () => {
    expect(new EmpAdditionalDesignation()).toBeTruthy();
  });
});
