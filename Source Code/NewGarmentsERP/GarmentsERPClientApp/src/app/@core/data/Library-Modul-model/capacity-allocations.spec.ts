import { CapacityAllocations } from './capacity-allocations';

describe('CapacityAllocations', () => {
  it('should create an instance', () => {
    expect(new CapacityAllocations()).toBeTruthy();
  });
});
