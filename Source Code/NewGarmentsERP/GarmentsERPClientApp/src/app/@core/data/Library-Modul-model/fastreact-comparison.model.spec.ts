import { FastreactComparison } from './fastreact-comparison.model';

describe('FastreactComparison', () => {
  it('should create an instance', () => {
    expect(new FastreactComparison()).toBeTruthy();
  });
});
