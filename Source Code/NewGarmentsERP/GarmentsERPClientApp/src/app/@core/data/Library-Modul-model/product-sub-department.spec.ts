import { ProductSubDepartment } from './product-sub-department';

describe('ProductSubDepartment', () => {
  it('should create an instance', () => {
    expect(new ProductSubDepartment()).toBeTruthy();
  });
});
