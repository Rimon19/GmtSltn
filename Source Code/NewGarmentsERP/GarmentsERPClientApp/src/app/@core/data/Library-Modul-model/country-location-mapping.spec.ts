import { CountryLocationMapping } from './country-location-mapping';

describe('CountryLocationMapping', () => {
  it('should create an instance', () => {
    expect(new CountryLocationMapping()).toBeTruthy();
  });
});
