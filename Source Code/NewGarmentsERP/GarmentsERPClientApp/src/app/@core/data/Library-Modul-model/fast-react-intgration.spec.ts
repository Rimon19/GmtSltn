import { FastReactIntgration } from './fast-react-intgration';

describe('FastReactIntgration', () => {
  it('should create an instance', () => {
    expect(new FastReactIntgration()).toBeTruthy();
  });
});
