import { GarmentsItemEntries } from './garments-item-entries';

describe('GarmentsItemEntries', () => {
  it('should create an instance', () => {
    expect(new GarmentsItemEntries()).toBeTruthy();
  });
});
