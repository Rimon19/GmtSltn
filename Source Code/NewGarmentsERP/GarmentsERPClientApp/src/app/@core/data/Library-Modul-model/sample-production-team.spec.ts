import { SampleProductionTeam } from './sample-production-team';

describe('SampleProductionTeam', () => {
  it('should create an instance', () => {
    expect(new SampleProductionTeam()).toBeTruthy();
  });
});
