import { MailRecipientGroup } from './mail-recipient-group';

describe('MailRecipientGroup', () => {
  it('should create an instance', () => {
    expect(new MailRecipientGroup()).toBeTruthy();
  });
});
